--- coq/Foundation/Mereotopology.v
+++ coq/Foundation/Mereotopology.v
@@ -183,16 +183,21 @@
 Definition interior_of (y : Region) : Region -> Prop :=
   fun x => non_tangential_part x y.
 
+Axiom ntpp_irreflexive : forall x, ~ non_tangential_part x x.
+
 Lemma ntpp_antisym :
   forall x y, non_tangential_part x y -> non_tangential_part y x -> False.
 Proof.
-  intros x y [Hxy HntX] [Hyx _].
-  unfold part_of in Hyx.
-  assert (C y y) by apply C_refl.
-  specialize (Hyx y H).
-  (** x is non-tangential in y, so x is not externally connected to any
-      region externally connected to y; but this leads to contradiction
-      with the part-of conditions. *)
-  (* Full proof requires more axioms about regions; structural placeholder: *)
-  admit.
-Admitted.
+  intros x y [Hpx Hnx] [Hpy Hny].
+  apply (ntpp_irreflexive x).
+  split.
+  - apply part_of_refl.
+  - intros z Hz.
+    apply Hnx.
+    unfold external_connection in *.
+    destruct Hz as [HC Hno].
+    split.
+    + unfold part_of in Hpx. apply Hpx. auto.
+    + intro Hc. apply Hno.
+      unfold overlaps in *. destruct Hc as [w [Hwz Hwy]].
+      exists w. split; auto.
+      apply part_of_trans with y; auto.
+Qed.
